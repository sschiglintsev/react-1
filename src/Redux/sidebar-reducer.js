
let initialState ={
    friends: [
        {id: 1, name: 'Sergey'},
        {id: 2, name: 'Oleg'},
        {id: 3, name: 'Masha'}
    ]
}
const sidebarReducer = (state = initialState) => {
    return state;
}

export default sidebarReducer;